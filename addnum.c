#include <stdio.h>

/**
 * addnum - adds two numbers
 * @first: first number
 * @second: second number
 * Return: the total
 */
int addnum(int first, int second)
{
	int total;

	total = first + second;

	return (total);
}
